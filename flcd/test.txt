fn main() {
    let n: i32;
    let i: i32;
    let m.ax: i32;

    read(n);
    m.ax = 0$;
    for (i in 0..n) {
        let x: i32;
        read(x);

        if (x > m.ax) {
            m.ax = x;
        }
    }

    print(m.ax);
}